0成功

2-命令行问题、错误或非法标志或命令组合，或坏的环境变量。必须修改命令行。

8-建设中断，但我们终止了有序关闭。

32-此计算机上没有外部环境故障。

33-OOM故障。您需要修改命令行。

34-保留供谷歌内部使用。

35-保留供谷歌内部使用。

36-当地环境问题，疑似永久性。

37-未处理的异常/内部bazel错误。

38-保留供谷歌内部使用。

40-44-为bazel的命令行启动程序bazel.cc中与命令行无关的错误保留。通常这些都与bazel服务器无法启动自身有关。


bazel build，bazel test命令的返回代码。


1-生成失败。

3-生成正常，但有些测试失败或超时。

4-生成成功，但未找到任何测试，即使已请求测试。


对于bazel run：


1-生成失败。

如果生成成功，但执行的子进程返回一个非零的退出代码，则它也将是命令的退出代码。


对于Bazel查询：


3-部分成功，但查询在输入生成文件集中遇到1个或多个错误，因此操作结果不完全可靠。这可能是由于命令行中有一个--keep_going选项。

7-命令失败。
